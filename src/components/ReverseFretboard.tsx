import {useMemo,useState} from 'react';import {detectChords,detectScales} from '../music/compatibility';import {STANDARD_TUNING} from '../music/fretboard';import {getPitchClass,getPitchName,normalizePitchClass} from '../music/notes';
export function ReverseFretboard(){const[selected,setSelected]=useState<number[]>([]),chords=useMemo(()=>detectChords(selected),[selected]),scales=useMemo(()=>detectScales(selected),[selected]);function toggle(pitch:number){setSelected(current=>current.includes(pitch)?current.filter(p=>p!==pitch):[...current,pitch])}return <div className="reverse-tool"><p>Seleccioná notas sobre el mástil. Una misma altura se resalta en todas sus posiciones.</p><div className="reverse-scroll"><div className="reverse-board"><div className="reverse-frets"><span/>{Array.from({length:13},(_,f)=><span key={f}>{f}</span>)}</div>{[...STANDARD_TUNING].reverse().map((string,row)=><div className="reverse-string" key={`${string}-${row}`}><b>{string}</b>{Array.from({length:13},(_,fret)=>{const pitch=normalizePitchClass(getPitchClass(string)!+fret),active=selected.includes(pitch);return <button key={fret} aria-pressed={active} aria-label={`${getPitchName(pitch)}, cuerda ${string}, traste ${fret}`} onClick={()=>toggle(pitch)}><span>{getPitchName(pitch).replace('#','♯')}</span></button>})}</div>)}</div></div><div className="selected-pitches"><strong>Notas:</strong>{selected.length?[...selected].sort((a,b)=>a-b).map(p=><button key={p} onClick={()=>toggle(p)}>{getPitchName(normalizePitchClass(p))} ×</button>):<span>Ninguna seleccionada</span>}{selected.length>0&&<button className="clear-selection" onClick={()=>setSelected([])}>Limpiar</button>}</div><div className="detection-results"><section><h3>Acordes detectados</h3>{chords.length?<div className="detection-chips">{chords.map(chord=><span key={`${chord.root}-${chord.quality}`}><strong>{chord.label}</strong> {chord.notes.join(' ')}</span>)}</div>:<p>Seleccioná las notas exactas de un acorde.</p>}</section><section><h3>Escalas posibles</h3>{scales.length?<div className="detection-chips">{scales.map(scale=><span key={`${scale.root}-${scale.scaleKey}`} className={scale.exact?'exact':''}><strong>{scale.label}</strong>{scale.exact?' · coincidencia exacta':''}</span>)}</div>:<p>No hay coincidencias todavía.</p>}</section></div></div>}
