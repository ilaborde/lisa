import {useMemo,useState} from 'react'; import {ROOT_OPTIONS} from '../music/notes'; import {getIntervalBetween} from '../music/intervals';
export function IntervalTool(){const[a,setA]=useState('C'),[b,setB]=useState('E'),i=useMemo(()=>getIntervalBetween(a,b),[a,b]);return <div className="tool-card"><div className="tool-row"><label>Nota inicial<select value={a} onChange={e=>setA(e.target.value)}>{ROOT_OPTIONS.map(n=><option key={n}>{n}</option>)}</select></label><label>Nota final<select value={b} onChange={e=>setB(e.target.value)}>{ROOT_OPTIONS.map(n=><option key={n}>{n}</option>)}</select></label></div><div className="tool-result"><strong>{a} → {b}</strong><span>{i.semitones} semitonos · {i.description}</span><small>El nombre se presenta cromáticamente; el spelling puede distinguir intervalos enharmónicos.</small></div></div>}
