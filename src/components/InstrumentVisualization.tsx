import type {Instrument} from '../music/instrument';import type {ScaleKey} from '../music/scales';import {Fretboard} from './Fretboard';import {PianoKeyboard} from './PianoKeyboard';
export function InstrumentVisualization({instrument,root,scaleKey,labelMode,chordNotes=[]}:{instrument:Instrument;root:string;scaleKey:ScaleKey;labelMode:'notes'|'degrees'|'both';chordNotes?:readonly string[]}){return instrument==='guitar'?<Fretboard root={root} scaleKey={scaleKey} labelMode={labelMode} chordNotes={chordNotes}/>:<PianoKeyboard root={root} scaleKey={scaleKey} labelMode={labelMode} chordNotes={chordNotes}/>} 
