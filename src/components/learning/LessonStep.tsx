import type {LessonStep as Step} from '../../learning/lessonTypes';import {Exercise} from './Exercise';
export function LessonStep({step,onCorrect}:{step:Step;onCorrect:()=>void}){if(step.type==='exercise')return <article className="lesson-step"><p className="eyebrow">Ejercicio</p><h2>{step.title}</h2><Exercise step={step} onCorrect={onCorrect}/></article>;return <article className={`lesson-step ${step.type}`}><p className="eyebrow">{step.type==='summary'?'Resumen':'Descubrimiento'}</p><h2>{step.title}</h2><p>{step.body}</p>{step.lines&&<div className="learning-lines">{step.lines.map(line=><code key={line}>{line}</code>)}</div>}</article>}
