import {useMemo,useState} from 'react';import {detectChords,detectScales} from '../music/compatibility';import {buildChord,type ChordRecord} from '../music/chords';import {PianoKeyboard,PIANO_KEYS} from './PianoKeyboard';
export function PianoDetector({onUseChord}:{onUseChord?:(chord:ChordRecord)=>void}){const[selected,setSelected]=useState<number[]>([]),chords=useMemo(()=>detectChords(selected),[selected]),scales=useMemo(()=>detectScales(selected),[selected]),selectedKeys=PIANO_KEYS.filter(k=>selected.includes(k.pitchClass)).map(k=>k.id);return <div className="piano-detector"><p>Seleccioná alturas en el teclado. La misma nota queda marcada en todas las octavas.</p><PianoKeyboard root="C" scaleKey="major" labelMode="notes" selectedKeys={selectedKeys} onToggle={key=>setSelected(v=>v.includes(key.pitchClass)?v.filter(p=>p!==key.pitchClass):[...v,key.pitchClass])}/><div className="selected-pitches"><strong>Alturas seleccionadas:</strong><span>{selected.length||'Ninguna'}</span>{selected.length>0&&<button onClick={()=>setSelected([])}>Limpiar</button>}</div><div className="detection-results"><section><h3>Acordes detectados</h3>{chords.length?<div className="detection-chips">{chords.map(result=>{const chord=buildChord(result.root,result.quality);return <span key={`${result.root}-${result.quality}`}><strong>{result.label}</strong>{result.notes.join(' ')}{chord&&onUseChord&&<button onClick={()=>onUseChord(chord)}>Usar este acorde</button>}</span>})}</div>:<p>Elegí las notas exactas de un acorde.</p>}</section><section><h3>Escalas posibles</h3>{scales.length?<div className="detection-chips">{scales.map(scale=><span key={`${scale.root}-${scale.scaleKey}`} className={scale.exact?'exact':''}><strong>{scale.label}</strong>{scale.exact?' · coincidencia exacta':''}</span>)}</div>:<p>No hay coincidencias todavía.</p>}</section></div></div>}
