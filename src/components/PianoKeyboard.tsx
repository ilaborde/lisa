import {getIntervalBetween} from '../music/intervals';import {getPitchClass,getPitchName,normalizePitchClass} from '../music/notes';import {getScaleNotes,type ScaleKey} from '../music/scales';
export type PianoKey={id:string;pitchClass:number;note:string;black:boolean;octave:number};
const BLACK=new Set([1,3,6,8,10]);export const PIANO_KEYS:readonly PianoKey[]=Array.from({length:25},(_,i)=>{const midi=48+i,pitchClass=normalizePitchClass(midi),octave=Math.floor(midi/12)-1;return{id:`${midi}`,pitchClass,note:getPitchName(pitchClass),black:BLACK.has(pitchClass),octave}});
export function PianoKeyboard({root,scaleKey,chordNotes=[],labelMode='both',selectedKeys=[],onToggle}:{root:string;scaleKey:ScaleKey;chordNotes?:readonly string[];labelMode?:'notes'|'degrees'|'both';selectedKeys?:readonly string[];onToggle?:(key:PianoKey)=>void}){const scalePitches=new Set<number>(getScaleNotes(root,scaleKey).map(getPitchClass).filter((v):v is NonNullable<typeof v>=>v!==null)),rootPitch=getPitchClass(root),chordPitches=new Set<number>(chordNotes.map(getPitchClass).filter((v):v is NonNullable<typeof v>=>v!==null));return <div className="piano-scroll"><div className="piano-keyboard" role="group" aria-label="Teclado de piano">{PIANO_KEYS.map(key=>{const inScale=scalePitches.has(key.pitchClass),isRoot=key.pitchClass===rootPitch,chordTone=chordPitches.has(key.pitchClass),selected=selectedKeys.includes(key.id),degree=getIntervalBetween(root,key.note).degree;return <button type="button" key={key.id} className={`piano-key ${key.black?'black':'white'} ${inScale?'in-scale':''} ${isRoot?'root':''} ${chordTone?'chord-tone':''}`} aria-pressed={selected} onClick={()=>onToggle?.(key)}><span>{labelMode!=='degrees'&&`${key.note.replace('#','♯')}${key.octave}`}</span>{labelMode!=='notes'&&inScale&&<small>{degree}</small>}</button>})}</div></div>}
