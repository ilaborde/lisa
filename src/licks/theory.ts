import {getIntervalInfo} from '../music/intervals';import {getPitchClass,getPitchName,normalizePitchClass} from '../music/notes';import {getScaleByKey,getScaleDegreeNames,type ScaleKey} from '../music/scales';import {STANDARD_TUNING} from '../music/fretboard';import type {LickEvent,ReferenceLick} from './types';
export function pitchOfPosition(note:LickEvent){return normalizePitchClass(getPitchClass(STANDARD_TUNING[6-note.string])!+note.fret)}
export function noteNameOfPosition(note:LickEvent,flat=false){return getPitchName(pitchOfPosition(note),flat?'flat':'sharp')}
export function degreeOfPosition(note:LickEvent,root:string){return getIntervalInfo(pitchOfPosition(note)-(getPitchClass(root)??0)).degree}
export function analyzeTranscription(lick:ReferenceLick,baseScale?:ScaleKey){const scaleKey=baseScale??lick.musicalContext.scaleCandidates[0],scale=scaleKey?getScaleByKey(scaleKey):undefined,root=getPitchClass(lick.originalKey)??0,seen=new Set<string>(),outside=new Set<string>();for(const n of lick.transcription??[]){const interval=normalizePitchClass(pitchOfPosition(n)-root),degree=getIntervalInfo(interval).degree;seen.add(degree);if(scale&&!scale.formula.includes(interval))outside.add(`${noteNameOfPosition(n,lick.originalKey.includes('b'))} (${degree})`)}return {formula:scaleKey?getScaleDegreeNames(scaleKey):[],usedDegrees:[...seen],outsideNotes:[...outside]}}
export const analyzeLick=analyzeTranscription;
